const defaultState = {
    list: [],
    count: 5
};

const todoReducer = (state = defaultState, actions) => {
    switch (actions.type) {
        default:
            return state;
    }
};

export default todoReducer;
